# 03_CICD

This project uses **GitHub Actions** for CI.

---

## Workflow
- File: `.github/workflows/build.yml`
- Trigger: `push` to `main`, PRs, manual (`workflow_dispatch`)

---

## Current stage (MVP)
CI is running and validates repository structure.